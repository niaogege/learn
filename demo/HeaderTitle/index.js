import React from 'react';
import { HeaderTitle } from '@chendap/ui';
export default ({}) => {
    return (React.createElement("div", null,
        React.createElement(HeaderTitle, { title: "Test cpp" })));
};
