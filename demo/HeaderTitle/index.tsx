import React, { useState } from 'react';
import { HeaderTitle } from '@chendap/ui';

export default ({}) => {
    return (
        <div>
            <HeaderTitle title="Test cpp" />
        </div>
    );
};
