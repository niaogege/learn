import React, { useState } from 'react';
import { HeaderTitle } from '@niaogege/cpp-ui';

export default ({}) => {
    return (
        <div>
            <HeaderTitle title="Test cpp" />
        </div>
    );
};
